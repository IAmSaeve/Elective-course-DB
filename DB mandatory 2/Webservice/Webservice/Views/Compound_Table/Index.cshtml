@model IEnumerable<Webservice.Models.Compound_Table>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StofNavn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StofNavn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StofId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StofId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StofId })
        </td>
    </tr>
}

</table>
