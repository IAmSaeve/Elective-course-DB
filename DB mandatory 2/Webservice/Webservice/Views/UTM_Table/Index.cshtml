@model IEnumerable<Webservice.Models.UTM_Table>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UTMX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UTMY)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UTMZone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UTMX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UTMY)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UTMZone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GeometriId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GeometriId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GeometriId })
        </td>
    </tr>
}

</table>
